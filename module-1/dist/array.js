"use strict";
// /* const names:string[]=["rahim","karim","abul"];
// names.map(name=>name.) */  //if we want string name of array ,then we map by only on string method
// /* const rollNumbers:number[]=[34,54,65];
// rollNumbers.map(number=>number.) */ //if we want number of array ,then we map by only on number method
// //----------Tuples----------
// const users:[string,number]=["rahim",2];
// users[0]="karim";
// console.log(users);
